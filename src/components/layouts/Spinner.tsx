import React from "react";

const Spinner: React.FC = (): JSX.Element => (
    <div className="w-8 h-8 border-t-2 border-blue-500 border-solid rounded-full animate-spin m-auto"></div>
);

export default Spinner;